# =========================================================
# Robots.txt for Blog Ideas & Stories
# Updated: Public Access Enabled
# =========================================================

User-agent: *
Allow: /

# --- 1. Block Auth & Private User Areas ---
# Bots don't have accounts. Crawling these is useless.
Disallow: /login
Disallow: /signup
Disallow: /dashboard

# --- 2. Block Write/Edit Routes ---
# These pages require a session. Bots will hit a redirect or 401 error.
Disallow: /add-post
Disallow: /edit-post/

# --- 3. IMPORTANT: Public Content ---
# We have REMOVED '/all-posts' from the block list.
# Google is now allowed to crawl:
# - / (Home)
# - /all-posts (The library)
# - /post/:slug (The articles)
# - /author/:username (The writers)

# --- 4. Technical Assets ---
# Helps Google render your modern React layout correctly.
Allow: /*.css
Allow: /*.js
Allow: /*.png
Allow: /*.jpg
Allow: /*.webp
Allow: /*.svg

# --- 5. Sitemap ---
Sitemap: https://blogideasandstories.vercel.app/sitemap.xml